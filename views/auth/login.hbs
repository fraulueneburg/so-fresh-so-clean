<h1>Login</h1>
<small class="alert alert-secondary"><strong>Coming <u>so</u> soon:</strong> Demo Login for anyone reviewing Wiebke’s portfolio.<br> For now, just sign up with x@y.z or something. We won’t shame you, promise.</small>
<form action="/login" method="post" >
    {{#if error}}
        <p id="form-error" class="alert error" role="alert" aria-live="assertive">
            <strong>{{error}}</strong>
            {{#if description}}
                <br />{{description}}
            {{/if}}
        </p>
    {{/if}}
    <input type="email" name="email" value="" placeholder="Email" autocomplete="email" required {{#if error}}aria-errormessage="err-email"{{/if}} />
    <input type="password" name="password" autocomplete="current-password" value="" placeholder="*********" required />
    <input type="submit" value="Login" />
</form>
<p class="text-right">
    <a href="/signup">Create new account instead?</a>
</p>